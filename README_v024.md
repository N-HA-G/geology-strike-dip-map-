# Version 0.24.0

地図上で距離を測る機能を追加しました．

## 距離測定

- 「計測開始」を押し，地図上をクリックして測点を追加します．
- 2点以上で折れ線距離を計算し，各区間距離と合計距離を地図上へ表示します．
- 「1点戻す」で直前の測点を取り消せます．
- 「計測終了」で計測結果を残したまま編集を終了します．
- 「計測線を消去」で計測結果を削除します．
- 距離はLeafletの `map.distance` を用いた測地線距離です．
- 計測結果は作業JSONに保存・復元されます．

地質解釈線の描画中と距離計測中は同時操作できないようにしています．
