function displayStrike(item){return useCorrectedValues&&Number.isFinite(item.strikeTrue)?item.strikeTrue:item.strikeRaw;}
function displayDipDirection(item){return useCorrectedValues&&Number.isFinite(item.dipDirectionTrue)?item.dipDirectionTrue:item.dipDirectionRaw;}

let tableSortKey="createdOrder";
let tableSortAscending=true;
function comparePointIdNatural(a,b){
  return String(a??"").localeCompare(String(b??""),"ja",{numeric:true,sensitivity:"base"});
}
function getTableSortedMeasurements(){
  return [...measurements].sort((a,b)=>{
    let result=0;
    if(tableSortKey==="pointId") result=comparePointIdNatural(a.pointId,b.pointId);
    else result=(a.createdOrder??0)-(b.createdOrder??0);
    if(result===0) result=(a.createdOrder??0)-(b.createdOrder??0);
    return tableSortAscending?result:-result;
  });
}
function updateTableSortButtons(){
  document.querySelectorAll("[data-table-sort]").forEach(button=>{
    const active=button.dataset.tableSort===tableSortKey;
    button.classList.toggle("active",active);
    const indicator=button.querySelector(".sort-indicator");
    if(indicator) indicator.textContent=active?(tableSortAscending?"▲":"▼"):"↕";
    button.setAttribute("aria-sort",active?(tableSortAscending?"ascending":"descending"):"none");
  });
}
document.addEventListener("click",event=>{
  const button=event.target.closest("[data-table-sort]");
  if(!button)return;
  const key=button.dataset.tableSort;
  if(!["createdOrder","pointId"].includes(key))return;
  if(tableSortKey===key)tableSortAscending=!tableSortAscending;
  else{tableSortKey=key;tableSortAscending=true;}
  renderMeasurements();
});
function temporaryPointIcon(){return L.divIcon({className:"click-point-icon",html:'<svg xmlns="http://www.w3.org/2000/svg" width="26" height="26" viewBox="0 0 26 26"><circle cx="13" cy="13" r="7" fill="white" stroke="#b00020" stroke-width="2.5"/><circle cx="13" cy="13" r="2.5" fill="#b00020"/></svg>',iconSize:[26,26],iconAnchor:[13,13]});}
function positionPointIcon(color="#8a5b00"){const c=normalizeHexColor(color,"#8a5b00");return L.divIcon({className:"position-point-icon",html:`<svg xmlns="http://www.w3.org/2000/svg" width="30" height="30" viewBox="0 0 30 30"><circle cx="15" cy="15" r="7" fill="#fff" stroke="${c}" stroke-width="3"/><circle cx="15" cy="15" r="2.5" fill="${c}"/></svg>`,iconSize:[30,30],iconAnchor:[15,15]});}
function strikeDipSvg(item,color="#111111"){
  const strike=displayStrike(item),dipDirection=displayDipDirection(item),dip=item.dip;const c=normalizeHexColor(color);const scale=symbolScalePercent/100;const size=104*scale,center=size/2;const strikeHalf=23*scale,tick=14*scale,r=Math.PI/180;const sr=strike*r,dr=dipDirection*r;const sx=Math.sin(sr),sy=-Math.cos(sr),dx=Math.sin(dr),dy=-Math.cos(dr);const x1=center-sx*strikeHalf,y1=center-sy*strikeHalf,x2=center+sx*strikeHalf,y2=center+sy*strikeHalf,tx=center+dx*tick,ty=center+dy*tick;const dipX=center+dx*26*scale,dipY=center+dy*26*scale;const idX=center-dx*31*scale,idY=center-dy*31*scale;const strikeX=x2+sx*13*scale,strikeY=y2+sy*13*scale;
  const font='BIZ UDPGothic, BIZ UDPゴシック, sans-serif';
  return `<svg xmlns="http://www.w3.org/2000/svg" width="${size}" height="${size}" viewBox="0 0 ${size} ${size}"><line x1="${x1}" y1="${y1}" x2="${x2}" y2="${y2}" stroke="${c}" stroke-width="${2.4*scale}" stroke-linecap="round"/><line x1="${center}" y1="${center}" x2="${tx}" y2="${ty}" stroke="${c}" stroke-width="${2.4*scale}" stroke-linecap="round"/>${showDipValue?`<text x="${dipX}" y="${dipY}" text-anchor="middle" dominant-baseline="middle" font-family="${font}" font-size="${12*scale}" font-weight="700" fill="${c}" stroke="#fff" stroke-width="${3*scale}" paint-order="stroke">${Number(dip.toFixed(1))}</text>`:""}${showPointId?`<text x="${idX}" y="${idY}" text-anchor="middle" dominant-baseline="middle" font-family="${font}" font-size="${11*scale}" font-weight="700" fill="${c}" stroke="#fff" stroke-width="${3*scale}" paint-order="stroke">${escapeHtml(item.pointId)}</text>`:""}${showStrikeValue?`<text x="${strikeX}" y="${strikeY}" text-anchor="middle" dominant-baseline="middle" font-family="${font}" font-size="${10*scale}" font-weight="700" fill="${c}" stroke="#fff" stroke-width="${3*scale}" paint-order="stroke">${Math.round(strike)}°</text>`:""}</svg>`;
}
function buildMarker(item){const color=pointSymbolColor(item);const icon=item.hasAttitude?L.divIcon({className:"strike-dip-icon",html:strikeDipSvg(item,color),iconSize:[104*(symbolScalePercent/100),104*(symbolScalePercent/100)],iconAnchor:[52*(symbolScalePercent/100),52*(symbolScalePercent/100)]}):positionPointIcon(color);item.marker=L.marker([item.latitude,item.longitude],{icon,title:item.pointId});item.marker.bindPopup(popupHtml(item));if(item.visible!==false)item.marker.addTo(measurementLayer);}
function rebuildMarker(item){if(item.marker)measurementLayer.removeLayer(item.marker);buildMarker(item);scheduleMarkerLayout();}
function rebuildAllMeasurementMarkers(){measurements.forEach(rebuildMarker);scheduleMarkerLayout();}
function popupHtml(item){const att=item.hasAttitude?`入力走向．${escapeHtml(item.rawStrike)}<br>入力傾斜．${escapeHtml(item.rawDip)}<br>元走向．${item.strikeRaw.toFixed(1)}°<br>補正走向．${item.strikeTrue.toFixed(1)}°<br>傾斜．${item.dip.toFixed(1)}°<br>元傾斜方向．${item.dipDirectionRaw.toFixed(1)}°<br>補正傾斜方向．${item.dipDirectionTrue.toFixed(1)}°<br>偏角．${item.declinationSigned.toFixed(1)}°（東＋／西−）`:`<strong>走向・傾斜未入力</strong>`;const extra=[item.surveyDate&&`測定日．${escapeHtml(item.surveyDate)}`,item.lithology&&`岩種・地層名．${escapeHtml(item.lithology)}`,item.structureType&&`面・構造．${escapeHtml(item.structureType)}`,item.notes&&`補足情報．${escapeHtml(item.notes).replaceAll("\n","<br>")}`].filter(Boolean).join("<br>");return `<strong>${escapeHtml(item.pointId)}</strong><br>緯度．${item.latitude.toFixed(6)}<br>経度．${item.longitude.toFixed(6)}<br>${att}${extra?`<br>${extra}`:""}`;}

function createPoint(data){const legacyStrike=data.strikeRaw??data.strike??null,legacyDipDir=data.dipDirectionRaw??data.dipDirection??null,dec=Number(data.declinationSigned??0);const item={id:data.id??nextInternalId++,createdOrder:data.createdOrder??nextCreatedOrder++,pointId:data.pointId||`P${String(nextCreatedOrder-1).padStart(3,"0")}`,latitude:Number(data.latitude),longitude:Number(data.longitude),source:data.source||"手動",sourceFile:data.sourceFile||"",sourceType:data.sourceType||"manual",hasAttitude:Boolean(data.hasAttitude),rawStrike:data.rawStrike||"",rawDip:data.rawDip||"",strikeRaw:legacyStrike,dip:data.dip??null,dipDirectionRaw:legacyDipDir,dipDirectionLabel:data.dipDirectionLabel||"",declinationSigned:dec,strikeTrue:data.strikeTrue??correctedAzimuth(legacyStrike,dec),dipDirectionTrue:data.dipDirectionTrue??correctedAzimuth(legacyDipDir,dec),surveyDate:data.surveyDate||"",lithology:data.lithology||"",structureType:data.structureType||"",notes:data.notes||"",colorDefinitionId:data.colorDefinitionId||"default",gpxTime:data.gpxTime||"",visible:data.visible!==false,marker:null};measurements.push(item);buildMarker(item);nextInternalId=Math.max(nextInternalId,item.id+1);nextCreatedOrder=Math.max(nextCreatedOrder,item.createdOrder+1);return item;}
function plainPoint(i){return {id:i.id,createdOrder:i.createdOrder,pointId:i.pointId,latitude:i.latitude,longitude:i.longitude,source:i.source,sourceFile:i.sourceFile,sourceType:i.sourceType,hasAttitude:i.hasAttitude,rawStrike:i.rawStrike,rawDip:i.rawDip,strikeRaw:i.strikeRaw,dip:i.dip,dipDirectionRaw:i.dipDirectionRaw,dipDirectionLabel:i.dipDirectionLabel,declinationSigned:i.declinationSigned,strikeTrue:i.strikeTrue,dipDirectionTrue:i.dipDirectionTrue,surveyDate:i.surveyDate,lithology:i.lithology,structureType:i.structureType,notes:i.notes,colorDefinitionId:i.colorDefinitionId,gpxTime:i.gpxTime,visible:i.visible!==false};}

function markerCandidateOffsets(symbolSize,gap){const out=[[0,0]],step=Math.max(24,symbolSize+gap);for(let ring=1;ring<=8;ring++){const radius=step*ring;for(let n=0;n<20;n++){const a=Math.PI*2*n/20-Math.PI/2;out.push([Math.cos(a)*radius,Math.sin(a)*radius]);}}return out;}
function boxesOverlap(a,b){return !(a.right<=b.left||a.left>=b.right||a.bottom<=b.top||a.top>=b.bottom);}
function setMarkerActual(item){if(item.marker)item.marker.setLatLng([item.latitude,item.longitude]);}
function updateMarkerLayout(){declutterUpdateQueued=false;declutterLeaderLayer.clearLayers();measurements.forEach(setMarkerActual);if(!autoDeclutterEnabled)return;const occupied=[],symbolSize=104*(symbolScalePercent/100),half=symbolSize/2+declutterGapPx/2,candidates=markerCandidateOffsets(symbolSize,declutterGapPx);measurements.filter(i=>i.hasAttitude&&i.visible!==false&&i.marker&&measurementLayer.hasLayer(i.marker)).sort((a,b)=>a.createdOrder-b.createdOrder).forEach(item=>{const actual=L.latLng(item.latitude,item.longitude),p=map.latLngToLayerPoint(actual);let selected=[0,0],box=null;for(const off of candidates){const b={left:p.x+off[0]-half,right:p.x+off[0]+half,top:p.y+off[1]-half,bottom:p.y+off[1]+half};if(!occupied.some(x=>boxesOverlap(b,x))){selected=off;box=b;break;}}if(!box)return;occupied.push(box);if(Math.hypot(...selected)<1)return;const display=map.layerPointToLatLng(L.point(p.x+selected[0],p.y+selected[1]));item.marker.setLatLng(display);const c=pointSymbolColor(item);L.polyline([actual,display],{color:c,weight:1.2,opacity:.75,dashArray:"4,3",interactive:false}).addTo(declutterLeaderLayer);L.circleMarker(actual,{radius:2.4,color:c,weight:1,fillColor:c,fillOpacity:1,interactive:false}).addTo(declutterLeaderLayer);});}
function scheduleMarkerLayout(){if(declutterUpdateQueued)return;declutterUpdateQueued=true;requestAnimationFrame(updateMarkerLayout);}
map.on("zoomend moveend resize",()=>{zoomSlider.value=String(map.getZoom());zoomValue.textContent=Number(map.getZoom()).toFixed(2);scheduleMarkerLayout();});
zoomSlider.addEventListener("input",()=>map.setZoom(Number(zoomSlider.value)));

function updateDisplaySettings(){showPointId=showPointIdInput.checked;showDipValue=showDipValueInput.checked;showStrikeValue=showStrikeValueInput.checked;useCorrectedValues=useCorrectedValuesInput.checked;symbolScalePercent=clamp(Number(symbolSizeInput.value)||100,50,220);symbolSizeValue.textContent=`${symbolScalePercent}％`;autoDeclutterEnabled=autoDeclutterInput.checked;declutterGapPx=clamp(Number(declutterGapInput.value)||0,0,50);declutterGapValue.textContent=`${declutterGapPx} px`;rebuildAllMeasurementMarkers();}
[showPointIdInput,showDipValueInput,showStrikeValueInput,useCorrectedValuesInput,autoDeclutterInput].forEach(el=>el.addEventListener("change",updateDisplaySettings));symbolSizeInput.addEventListener("input",updateDisplaySettings);declutterGapInput.addEventListener("input",updateDisplaySettings);

map.on("click",e=>{latInput.value=e.latlng.lat.toFixed(6);lngInput.value=e.latlng.lng.toFixed(6);if(temporaryMarker)temporaryMarker.setLatLng(e.latlng);else temporaryMarker=L.marker(e.latlng,{icon:temporaryPointIcon(),interactive:false}).addTo(map);msg.textContent=`クリック地点を選択しました．緯度 ${latInput.value}，経度 ${lngInput.value}．`;msg.className="msg success";});
function resetForm(){editingId=null;formTitle.textContent="ポイントを追加";savePointButton.textContent="ポイントを追加";cancelEditButton.hidden=true;strikeTextInput.value="";dipTextInput.value="";surveyDateInput.value="";lithologyInput.value="";structureTypeInput.value="";colorGroupInput.value="default";notesInput.value="";parseAttitudeInputs(false);}
function startEditing(item){editingId=item.id;formTitle.textContent=`ポイントを編集．${item.pointId}`;savePointButton.textContent="ポイントを更新";cancelEditButton.hidden=false;pointIdInput.value=item.pointId;latInput.value=item.latitude.toFixed(6);lngInput.value=item.longitude.toFixed(6);strikeTextInput.value=item.rawStrike;dipTextInput.value=item.rawDip;surveyDateInput.value=item.surveyDate;lithologyInput.value=item.lithology;structureTypeInput.value=item.structureType;colorGroupInput.value=item.colorDefinitionId;notesInput.value=item.notes;parseAttitudeInputs(false);map.setView([item.latitude,item.longitude],Math.max(map.getZoom(),17));}
cancelEditButton.addEventListener("click",resetForm);
pointForm.addEventListener("submit",e=>{e.preventDefault();const lat=Number(latInput.value),lng=Number(lngInput.value);if(!Number.isFinite(lat)||lat<-90||lat>90||!Number.isFinite(lng)||lng<-180||lng>180){msg.textContent="緯度・経度を確認してください．";msg.className="msg error";return;}const att=parseAttitudeInputs();if(!att)return;const pointId=pointIdInput.value.trim()||`P${String(nextCreatedOrder).padStart(3,"0")}`;const action=editingId!==null?`地点「${pointId}」を更新します．`:`地点「${pointId}」を追加します．`;if(!confirmAction(action))return;if(editingId!==null){const item=measurements.find(i=>i.id===editingId);if(!item)return;Object.assign(item,{pointId,latitude:lat,longitude:lng,...att,surveyDate:surveyDateInput.value,lithology:lithologyInput.value.trim(),structureType:structureTypeInput.value.trim(),colorDefinitionId:colorGroupInput.value||"default",notes:notesInput.value.trim()});rebuildMarker(item);tableDrafts.delete(item.id);msg.textContent=`${pointId} を更新しました．`;resetForm();}else{createPoint({pointId,latitude:lat,longitude:lng,source:"手動",sourceType:"manual",...att,surveyDate:surveyDateInput.value,lithology:lithologyInput.value.trim(),structureType:structureTypeInput.value.trim(),colorDefinitionId:colorGroupInput.value||"default",notes:notesInput.value.trim(),visible:true});msg.textContent=`${pointId} を追加しました．`;resetForm();}if(temporaryMarker){map.removeLayer(temporaryMarker);temporaryMarker=null;}msg.className="msg success";renderMeasurements();});

applyDeclinationAllButton.addEventListener("click",()=>{const signed=currentDeclinationSigned();const label=signed<0?`西偏 ${Math.abs(signed)}°`:`東偏 ${signed}°`;if(!confirmAction(`全地点へ ${label} の偏角補正を適用します．元の走向・傾斜は保持されます．`))return;globalDeclinationSigned=signed;measurements.forEach(i=>{i.declinationSigned=signed;if(i.hasAttitude){i.strikeTrue=correctedAzimuth(i.strikeRaw,signed);i.dipDirectionTrue=correctedAzimuth(i.dipDirectionRaw,signed);}});rebuildAllMeasurementMarkers();renderMeasurements();parseAttitudeInputs(false);msg.textContent=`全地点へ ${label} を適用しました．`;msg.className="msg success";});

function renderMeasurements(){count.textContent=String(measurements.length);completeCount.textContent=String(measurements.filter(i=>i.hasAttitude).length);exportCsvButton.disabled=exportGeoJsonButton.disabled=exportPointExcelButton.disabled=measurements.length===0;pointTableBody.innerHTML="";if(!measurements.length){pointTableBody.innerHTML='<tr><td colspan="19" class="empty">まだポイントはありません．</td></tr>';updateDraftState();updateTableSortButtons();return;}getTableSortedMeasurements().forEach((item,index)=>{const draft=tableDrafts.get(item.id)||{};const val=k=>Object.prototype.hasOwnProperty.call(draft,k)?draft[k]:item[k]??"";const row=document.createElement("tr");if(tableDrafts.has(item.id))row.classList.add("draft-row");row.innerHTML=`<td>${index+1}</td><td><input class="visibility-checkbox" type="checkbox" data-visible-id="${item.id}" ${item.visible!==false?"checked":""}></td><td><input class="table-editor" data-edit-id="${item.id}" data-field="pointId" value="${escapeHtml(val("pointId"))}"></td><td>${item.hasAttitude?"入力済み":"未入力"}</td><td>${escapeHtml(item.source)}</td><td><input class="table-editor" type="number" step="0.000001" data-edit-id="${item.id}" data-field="latitude" value="${escapeHtml(val("latitude"))}"></td><td><input class="table-editor" type="number" step="0.000001" data-edit-id="${item.id}" data-field="longitude" value="${escapeHtml(val("longitude"))}"></td><td><input class="table-editor" data-edit-id="${item.id}" data-field="rawStrike" value="${escapeHtml(val("rawStrike"))}"></td><td><input class="table-editor" data-edit-id="${item.id}" data-field="rawDip" value="${escapeHtml(val("rawDip"))}"></td><td>${item.strikeRaw==null?"":item.strikeRaw.toFixed(1)}</td><td>${item.strikeTrue==null?"":item.strikeTrue.toFixed(1)}</td><td>${item.dip==null?"":item.dip.toFixed(1)}</td><td>${item.dipDirectionTrue==null?"":item.dipDirectionTrue.toFixed(1)}</td><td><input class="table-editor" type="date" data-edit-id="${item.id}" data-field="surveyDate" value="${escapeHtml(val("surveyDate"))}"></td><td><input class="table-editor" data-edit-id="${item.id}" data-field="lithology" value="${escapeHtml(val("lithology"))}"></td><td><input class="table-editor" data-edit-id="${item.id}" data-field="structureType" value="${escapeHtml(val("structureType"))}"></td><td><select class="table-editor" data-edit-id="${item.id}" data-field="colorDefinitionId">${colorDefinitions.map(d=>`<option value="${escapeHtml(d.id)}" ${d.id===val("colorDefinitionId")?"selected":""}>${escapeHtml(d.name)}</option>`).join("")}</select></td><td><input class="table-editor" data-edit-id="${item.id}" data-field="notes" value="${escapeHtml(val("notes"))}"></td><td><div class="table-actions"><button data-action="focus" data-id="${item.id}">地図へ</button><button data-action="edit" data-id="${item.id}">編集</button><button class="danger" data-action="delete" data-id="${item.id}">削除</button></div></td>`;pointTableBody.appendChild(row);});updateDraftState();updateTableSortButtons();scheduleMarkerLayout();}
function updateDraftState(){const n=tableDrafts.size;applyTableChangesButton.disabled=discardTableChangesButton.disabled=n===0;draftStatus.textContent=n?`未反映の変更．${n}地点`:"未反映の変更なし";draftStatus.classList.toggle("dirty",n>0);}
pointTableBody.addEventListener("input",e=>{const x=e.target.closest("[data-edit-id][data-field]");if(!x)return;const id=Number(x.dataset.editId),d=tableDrafts.get(id)||{};d[x.dataset.field]=x.value;tableDrafts.set(id,d);updateDraftState();x.closest("tr")?.classList.add("draft-row");});
pointTableBody.addEventListener("change",e=>{const v=e.target.closest(".visibility-checkbox[data-visible-id]");if(!v)return;const item=measurements.find(i=>i.id===Number(v.dataset.visibleId));if(!item)return;item.visible=v.checked;if(item.visible){if(item.marker&&!measurementLayer.hasLayer(item.marker))item.marker.addTo(measurementLayer);}else if(item.marker&&measurementLayer.hasLayer(item.marker))measurementLayer.removeLayer(item.marker);scheduleMarkerLayout();});
pointTableBody.addEventListener("click",e=>{const b=e.target.closest("button[data-action]");if(!b)return;const item=measurements.find(i=>i.id===Number(b.dataset.id));if(!item)return;if(b.dataset.action==="focus"){map.setView([item.latitude,item.longitude],Math.max(map.getZoom(),17));item.marker?.openPopup();}if(b.dataset.action==="edit")startEditing(item);if(b.dataset.action==="delete"){if(!confirmAction(`地点「${item.pointId}」を削除します．`))return;if(item.marker)measurementLayer.removeLayer(item.marker);measurements=measurements.filter(i=>i.id!==item.id);tableDrafts.delete(item.id);renderMeasurements();}});
applyTableChangesButton.addEventListener("click",()=>{if(!tableDrafts.size)return;if(!confirmAction(`${tableDrafts.size}地点の一覧変更を反映します．`))return;try{for(const [id,draft] of tableDrafts){const item=measurements.find(i=>i.id===id);if(!item)continue;const rawStrike=Object.prototype.hasOwnProperty.call(draft,"rawStrike")?draft.rawStrike:item.rawStrike,rawDip=Object.prototype.hasOwnProperty.call(draft,"rawDip")?draft.rawDip:item.rawDip,att=parseAttitudePair(rawStrike,rawDip,item.declinationSigned);Object.assign(item,{pointId:String(draft.pointId??item.pointId).trim()||item.pointId,latitude:Number(draft.latitude??item.latitude),longitude:Number(draft.longitude??item.longitude),...att,surveyDate:String(draft.surveyDate??item.surveyDate),lithology:String(draft.lithology??item.lithology),structureType:String(draft.structureType??item.structureType),notes:String(draft.notes??item.notes),colorDefinitionId:String(draft.colorDefinitionId??item.colorDefinitionId)});rebuildMarker(item);}tableDrafts.clear();renderMeasurements();msg.textContent="一覧変更を反映しました．";msg.className="msg success";}catch(error){msg.textContent=error.message;msg.className="msg error";}});
discardTableChangesButton.addEventListener("click",()=>{if(!tableDrafts.size)return;if(!confirmAction("未反映の一覧変更を破棄します．"))return;tableDrafts.clear();renderMeasurements();});
$("clearPoints").addEventListener("click",()=>{if(!measurements.length)return;if(!confirmAction("登録ポイントをすべて削除します．"))return;measurementLayer.clearLayers();declutterLeaderLayer.clearLayers();measurements=[];tableDrafts.clear();resetForm();renderMeasurements();});

